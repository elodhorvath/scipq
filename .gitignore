# directories
dist/

# built binaries — release assets only, never committed
# (anchored to repo root so the cmd/scipq/ source directory is not ignored)
/scipq
/scipq.exe
*.exe

# Go
*.test
*.out
coverage.txt
